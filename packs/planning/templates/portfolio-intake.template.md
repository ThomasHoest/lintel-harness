# Portfolio intake — Now / Next / Later

> The intake pass over the whole register. Same five fields as the
> roadmap review, used at the other end of the loop: the review closes a
> committed milestone, this one opens the next cycle. The fields are
> **fixed** — do not add, drop or rename them.
>
> The register this maintains is `portfolio/register.md`.

Date: `<date>` · Owner: `<name>` · Cadence trigger: `<what moved>`

---

## 1. What changed since last review

<Facts about the world and the portfolio since the last intake pass. New
opportunities arriving, constraints moving, capacity changing. Not a
status report of work in progress.>

## 2. Which bets met kill criteria

**Checked before anything new is let in.** Intake that adds without
subtracting is how a portfolio stops being absorbable.

| Bet | Any criterion crossed? | Outcome |
|---|---|---|
| `<slug>` | `<K1, on <date>>` | `<killed | re-committed (dated) | continues>` |

## 3. Absorption capacity check

<What the portfolio can absorb, computed **before** the Now/Next/Later
placement below, not after. Concentration is the goal: cut to absorbable
capacity, not to available capacity, and never to what fits on a slide.>

- Review throughput available this cycle: `<...>`
- Maintenance ownership available: `<...>`
- Security review capacity: `<...>`
- **Absorbable bet count this cycle:** `<n>`

## 4. Horizon check

<Which horizons the placement below assumes, and whether they hold.
A `Now` placement whose horizon is long, or a `Later` placement whose
horizon is short, is a contradiction — resolve it here.>

| Entry | Horizon | Binding determinant | Placement it implies |
|---|---|---|---|
| `<...>` | `<range>` | `<lead time | irreversibility>` | `<Now | Next | Later>` |

## 5. Intake gate

**Nothing enters without passing this gate.** An entry with no stated
intake reason is not an entry.

| Entry | Why now (the gate) | Origin | Placement |
|---|---|---|---|
| `<name>` | `<the reason this is in front of us now>` | `<where it came from>` | `<Now | Next | Later>` |

**Placed below the line, and why:**

| Entry | Why not | Revisit when |
|---|---|---|
| `<name>` | `<...>` | `<the signal that brings it back>` |

> A cut with no record is a cut that will be relitigated. One line each is
> enough.
